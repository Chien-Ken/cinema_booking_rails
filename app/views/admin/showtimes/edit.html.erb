<div class="max-w-2xl mx-auto mt-10 px-4">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-white">Edit Schedule</h1>
    <p class="text-zinc-400 mt-2">Changing time for <span class="text-blue-400"><%= @showtime.movie.title %></span>.</p>
  </div>

  <%= render 'form', showtime: @showtime %>
</div>
