<div class="max-w-4xl mx-auto py-10 px-4">
  <div class="mb-8">
    <%= link_to "← Back to Movies", admin_movies_path, class: "text-slate-500 hover:text-white transition" %>
    <h1 class="text-3xl font-bold text-white mt-4 text-glow">Edit: <%= @movie.title %></h1>
  </div>
  <%= render "form", movie: @movie %>
</div>
